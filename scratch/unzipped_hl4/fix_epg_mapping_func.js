const fs = require('fs');
let html = fs.readFileSync('consumet.html', 'utf8');

// The block we just inserted is currently at line 5723... Wait, I need to find what I inserted and replace it.
// Actually, let's just make the function.
